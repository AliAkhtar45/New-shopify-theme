<section class="section-block">
  <div class="container password-shell">
    <div class="password-card">
      <p class="eyebrow">404</p>
      <h1 class="detail-title">Page not found</h1>
      <p class="detail-intro">The page you were looking for does not exist. Continue shopping from the main store.</p>
      <a href="{{ routes.root_url }}" class="button button-primary">Back to home</a>
      <a href="{{ routes.all_products_collection_url }}" class="button button-secondary">View all products</a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main 404",
  "settings": []
}
{% endschema %}
